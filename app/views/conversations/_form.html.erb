

<%= form_for :conversation, url: :conversations, html: { class: "" } do |f| %>
    <div class="form-group" style="text-align:center;">


      <%= f.label :recipients %>

      <%= f.select(:recipients, User.all.collect {|p| [ p.username , p.id ] }, {}, { multiple: true , class: "form-control" })%>
    </div>
    <div class="form-group" style="text-align:center;">
      <%= f.label :subject %>
      <%= f.text_field :subject, placeholder: "Subject", class: "form-control" %>
    </div>
    <div class="form-group" style="text-align:center;">
      <%= f.label :message %>
      <%= f.text_area :body, class: 'form-control',placeholder: "Type your message here", rows: 5  %>
    </div>
<div class="container" style="text-align:center;">
    <%= f.submit "Send Message", class: "btn-treehouse"  %>
</div>
<% end %>
